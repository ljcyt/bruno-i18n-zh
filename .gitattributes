# 默认按文本处理，提交时规范化为 LF
* text=auto eol=lf

# PowerShell 脚本：保留 CRLF（且文件带 UTF-8 BOM，供 Windows PowerShell 5.1 正确识别中文）
*.ps1 text eol=crlf

# JSON / JS / MD 用 LF
*.json text eol=lf
*.js   text eol=lf
*.md   text eol=lf
